<?xml version="1.0" encoding="utf-8"?>
<!-- The launch screen before Flutter draws: the splash artwork's blue, so the
     hand-off to the full artwork (SplashOverlay) is seamless. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/lbm_splash" />
</layer-list>
